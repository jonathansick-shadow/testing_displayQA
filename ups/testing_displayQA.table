setupRequired(afw >= 4.6.0.0)
setupRequired(utils >= 4.6.0.0)

setupRequired(numpy >= 1.6.1+2)
setupRequired(matplotlib >= 1.0.1+2)
setupRequired(pysqlite >= 2.6.3+1)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
envAppend(PATH, ${PRODUCT_DIR}/bin)
